default:
  app:
    name: Log Service
    version: 1.0.0

  broker:
    amqp:
      exchange: headlessExchange
      username: rabbitmq
      password: rabbitmq
      host: localhost:15672
      port:
      topics: ["log.users"]
      queue: log
      queueOptions:
        exclusive: false
        durable: true
        autoDelete: false

development:

test:

staging:

production:
